const API="https://achados.onrender.com";async function carregar(){const id=new URLSearchParams(location.search).get("id");if(!id){produto.textContent="Produto não informado.";return}try{const r=await fetch(API+"/api/produtos/"+encodeURIComponent(id)),p=await r.json();if(!r.ok)throw 0;const imgs=[p.imagem,...(p.imagens||[])].filter(Boolean);produto.innerHTML='<div class="product-box"><div><img id="mainImg" class="main-img" src="'+esc(imgs[0])+'"><div class="thumbs">'+imgs.map(i=>'<img src="'+esc(i)+'" onclick="document.getElementById(\'mainImg\').src=\''+esc(i)+'\'">').join("")+'</div></div><div>'+(p.etiqueta?'<span class="tag">'+esc(p.etiqueta)+'</span>':"")+'<h1>'+esc(p.nome)+'</h1><div class="price">'+esc(p.preco||"Ver preço")+'</div><h2>Descrição</h2><p>'+br(p.descricao||"Sem descrição.")+'</p><h2>Especificações</h2><p>'+br(p.especificacoes||"Sem especificações.")+'</p><a class="buy" href="'+esc(p.linkAmazon)+'" target="_blank" rel="noopener noreferrer">🛒 Ver na Amazon</a></div></div>'}catch(e){produto.textContent="Produto não encontrado."}}function esc(s=""){return String(s).replace(/[&<>"']/g,m=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#039;"}[m]))}function br(s){return esc(s).replace(/\n/g,"<br>")}carregar();
