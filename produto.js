const API="https://achados.onrender.com";async function carregar(){const id=new URLSearchParams(location.search).get("id"),el=document.getElementById("produto");if(!id){el.textContent="Produto não informado.";return}try{const r=await fetch(API+"/api/produtos/"+encodeURIComponent(id)),p=await r.json();if(!r.ok)throw 0;const imgs=[p.imagem,...(Array.isArray(p.imagens)?p.imagens:[])].filter(Boolean);el.innerHTML='<div class="product-box"><div><img id="mainImg" class="main-img" src="'+esc(imgs[0])+'"><div class="thumbs">'+imgs.map(i=>'<img src="'+esc(i)+'" onclick="document.getElementById(\'mainImg\').src=\''+esc(i)+'\'">').join("")+'</div></div><div>'+(p.etiqueta?'<span class="tag">'+esc(p.etiqueta)+'</span>':"")+'<h1>'+esc(p.nome)+'</h1><div class="price">'+esc(p.preco||"Ver preço na Amazon")+'</div><h2>Descrição</h2><p>'+br(p.descricao||"Sem descrição.")+'</p><h2>Especificações</h2><p>'+br(p.especificacoes||"Sem especificações.")+'</p><a class="buy" href="'+esc(p.linkAmazon)+'" target="_blank" rel="noopener noreferrer">Ver na Amazon</a></div></div>'}catch(e){el.textContent="Não foi possível carregar este produto."}}function esc(s=""){return String(s).replace(/[&<>"']/g,m=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#039;"}[m]))}function br(s){return esc(s).replace(/\n/g,"<br>")}carregar();