const API="https://achados.onrender.com";let todos=[],catAtual="";async function carregar(){try{const[a,b]=await Promise.all([fetch(API+"/api/categorias"),fetch(API+"/api/produtos")]);const c=await a.json();todos=await b.json();categorias.innerHTML='<button class="cat active" onclick="filtrar(\'\')">Todos</button>'+c.map(x=>'<button class="cat" onclick="filtrar('+JSON.stringify(x.nome)+')">'+esc(x.nome)+'</button>').join("");render()}catch(e){status.textContent="Erro ao carregar produtos."}}function filtrar(c){catAtual=c;document.querySelectorAll(".cat").forEach(x=>x.classList.toggle("active",x.textContent===(c||"Todos")));render()}function render(){const l=catAtual?todos.filter(p=>p.categoria===catAtual):todos;status.textContent=l.length?l.length+" produto(s)":"Nenhum produto cadastrado.";produtos.innerHTML=l.map(p=>'<a class="card" href="produto.html?id='+encodeURIComponent(p._id)+'"><img src="'+esc(p.imagem)+'" alt="'+esc(p.nome)+'"><div class="card-body">'+(p.etiqueta?'<span class="tag">'+esc(p.etiqueta)+'</span>':"")+'<h3>'+esc(p.nome)+'</h3><div class="price">'+esc(p.preco||"Ver preço")+'</div></div></a>').join("")}function esc(s=""){return String(s).replace(/[&<>"']/g,m=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#039;"}[m]))}carregar();
