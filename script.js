const API="https://achados.onrender.com";let todos=[],categoriaAtual="";async function carregar(){try{const[a,b]=await Promise.all([fetch(API+"/api/categorias"),fetch(API+"/api/produtos")]);const cats=await a.json();todos=await b.json();renderCats(cats);renderProdutos()}catch(e){document.getElementById("status").textContent="Não foi possível carregar os produtos."}}function renderCats(cats){document.getElementById("categorias").innerHTML='<button class="cat active" onclick="filtrar(\'\')">Todos</button>'+cats.map(c=>'<button class="cat" onclick="filtrar('+JSON.stringify(c.nome)+')">'+esc(c.nome)+'</button>').join("")}function filtrar(c){categoriaAtual=c;renderProdutos()}function renderProdutos(){const lista=categoriaAtual?todos.filter(p=>p.categoria===categoriaAtual):todos;document.getElementById("status").textContent=lista.length?lista.length+" produto(s)":"Nenhum produto cadastrado.";document.getElementById("produtos").innerHTML=lista.map(p=>'<a class="card" href="produto.html?id='+encodeURIComponent(p._id)+'"><img src="'+esc(p.imagem)+'" alt="'+esc(p.nome)+'"><div class="card-body">'+(p.etiqueta?'<span class="tag">'+esc(p.etiqueta)+'</span>':"")+'<h3>'+esc(p.nome)+'</h3><div class="price">'+esc(p.preco||"Ver preço")+'</div></div></a>').join("")}function esc(s=""){return String(s).replace(/[&<>"']/g,m=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#039;"}[m]))}carregar();